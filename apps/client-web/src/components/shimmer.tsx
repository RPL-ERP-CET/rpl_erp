/* Shimmer Effect */
export default function Shimmer() {
    return (
        <div className="absolute inset-0 -translate-x-full [animation:shimmer_2s_infinite] bg-gradient-to-r from-transparent via-white/40 to-transparent" />
    );
}
